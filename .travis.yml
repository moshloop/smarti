language: go

go:
  - "1.9"

before_install: go get github.com/mitchellh/gox
script:  make
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  file:
    - smarti.zip
    - build/linux/smarti
    - build/windows/smarti.exe
  skip_cleanup: true
  on:
    tags: true